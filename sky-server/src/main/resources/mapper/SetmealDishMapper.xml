<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.SetmealDishMapper">

    <select id="queryByDishId" resultType="java.lang.Long">
        select * from setmeal_dish where dish_id in 
       <foreach collection="dishIds" item="dishId" separator="," open="(" close=")">
                                   #{dishId}
       </foreach>
    </select>

    <insert id="insertBySeMealId">
        insert into setmeal_dish (setmeal_id, dish_id, name, price, copies,create_time, update_time, create_user, update_user)
        VALUES
        <foreach collection="dishes" item="ds" separator=",">
            (#{ds.setmealId},#{ds.dishId},#{ds.name},#{ds.price},#{ds.copies},#{createTime},#{updateTime},
            #{createUser}, #{updateUser})
        </foreach>
    </insert>
</mapper>